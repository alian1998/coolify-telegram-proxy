# Add these to your support bot Coolify app (Getway-support-autoworker / gp-pay-backend).
# Copy PROXY_PASSWORD from the telegram-proxy Coolify env — do not commit real values.

TELEGRAM_HTTP_PROXY=http://tgproxy:YOUR_PROXY_PASS@88.222.213.240:18080

# telegramBot.js reads TELEGRAM_HTTP_PROXY first, then HTTP_PROXY / HTTPS_PROXY.
